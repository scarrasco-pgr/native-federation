{
  "name": "native-federation",
  "version": "1.0.0",
  "description": "Demo for native federation",
  "main": "index.js",
  "scripts": {
    "install:mismatch": "cd mismatch && npm install",
    "install:notes": "cd notes && npm install",
    "install:recipes": "cd recipes && npm install",
    "install:shell": "cd shell && npm install",
    "install:users": "cd users && npm install",
    "postinstall": "run-p install:*",
    "start:mismatch": "cd mismatch && npm start",
    "start:notes": "cd notes && npm start",
    "start:recipes": "cd recipes && npm start",
    "start:shell": "cd shell && npm start",
    "start:users": "cd users && npm start",
    "start": "run-p start:*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
